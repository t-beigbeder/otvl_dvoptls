---
dv:
  debug: false
  prod: true
  config_paths:
    delivery: /otvl/delivery
    data: /otvl/data
  localhosts:
    # fqdn: local-network-ip
    # eg:
    # t-k3s-sv-loc: "172.25.0.7"
  k3s:
    # hostname of the api server in a simple cluster
    # sc_api_server:
    # docker: true
    traefik:
      customize: true
  ctr:
    # release: ctr
    # skip_hdeploy: false
    # helm_uninstall: false
    # uid: 2001
    # fs_group: 2001
    # ingress_host: ctr.example.com
    # login: everybody
    # password: knows
    # apr_credentials: "everybody:$apr1$hd/Cq048$bx71KTBads2TZYNhCVzja1"

###